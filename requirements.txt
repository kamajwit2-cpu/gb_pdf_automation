# PDF Extraction Dependencies
pdfplumber==0.11.7
pandas==2.3.3
openpyxl==3.1.5
numpy>=1.26.0
Pillow>=9.1
flask==3.0.3

# SAP Integration Dependencies
pyrfc==3.0.0

# Production Server
waitress==3.0.0

# Note: numpy version is set to >=1.26.0 to ensure compatibility with both PDF processing and SAP integration
# All packages are compatible with Python 3.10 (required for pyrfc)